import { FirstMovieDirective } from './first-movie.directive';

describe('FirstMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstMovieDirective();
    expect(directive).toBeTruthy();
  });
});
